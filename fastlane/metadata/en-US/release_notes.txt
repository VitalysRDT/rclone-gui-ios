What's new in 1.3:
- Important fix: importing a password-encrypted rclone configuration ("rclone config encryption set") no longer crashes the app. Enter your password at import time — it is used once for decryption and never stored.
- New: "I have a code" button on the subscription screen to redeem promo codes.
- New: "Contact the Developer" page in Settings → Support.
- Completed English translation across the whole app.
- The app's source code is now public: github.com/VitalysRDT/rclone-gui-ios
